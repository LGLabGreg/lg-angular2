export const AppConfig: Object = {
  'api': {

  },
  'ui': {
    'forms': {
      'validationMessages': {
        'requiredForm': 'Please fill all the required fields',
        'requiredField': 'This field is required',
        'formSuccess': 'Thank you, the new user has been added to our database.',
        'invalidEmail': 'This email is invalid'
      },
      'validationPatterns': {
        'email': '[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,3}$'
      }
    }
  }
}